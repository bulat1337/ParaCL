row = 5;
col = 9;

arr = repeat(repeat(7, row), col);

row_id = 0;


while (row_id < row)
{
	col_id = 0;

	while (col_id < col)
	{
		print arr[col_id][row_id];

		col_id = col_id + 1;
	}

	row_id = row_id + 1;
}
